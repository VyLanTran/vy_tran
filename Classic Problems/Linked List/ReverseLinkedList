import java.util.Arrays;
import java.util.Iterator;
import java.util.LinkedList;

public class ReverseLinkedList {
	public static LinkedList reverse(LinkedList l) {
		LinkedList reversed = new LinkedList();
		for (int i = l.size() - 1; i >= 0; i--) {
			reversed.add(l.get(i));
		}
		return reversed;
	}
	
	public static void main(String args[]) {
		LinkedList l = new LinkedList<String>(Arrays.asList("a", "b", "c"));
		System.out.println(reverse(l));
		
		
		
		/* Using descendingIterator()
		LinkedList reversed = new LinkedList();
		Iterator iterator = l.descendingIterator();
		while (iterator.hasNext()) {
			reversed.add(iterator.next());
		}
		System.out.println(reversed);
		*/
	}
}
