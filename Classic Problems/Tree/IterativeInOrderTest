import java.util.Stack;

class IterativeInOrder {
	BinaryTree tree;
	Stack<Object> stk;
	
	public IterativeInOrder(BinaryTree tree) {
		this.tree = tree;
		stk = new Stack<Object>();
	}
	
	public void push(Node node) {
		if (node == null) {
			return;
		}
		push(node.right);
		stk.add(node.data);
		push(node.left);
	}
	
	public void inOrderTraversal() {
		push(tree.root);
		while (!stk.empty()) {
			System.out.print(stk.pop() + "\s");
		}
	}
}

public class IterativeInOrderTest {
	public static void main(String args[]) {
		BinaryTree tree = new BinaryTree();
		tree.root = new Node("A");
		tree.root.left = new Node("B");
		tree.root.right = new Node("C");
		tree.root.left.left = new Node("D");
		tree.root.left.right = new Node("E");
		tree.root.right.left = new Node("F");
		tree.root.left.left.left = new Node("G");
		
		IterativeInOrder ite = new IterativeInOrder(tree);
		ite.inOrderTraversal();
	}
}
